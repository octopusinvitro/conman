#!/usr/bin/env ruby

$LOAD_PATH.unshift File.join(File.dirname(__FILE__), '..', 'lib')

require 'console'
require 'ui'
require 'db'
require 'conman'

console = Console.new($stdin, $stdout)
conman  = Conman.new(UI.new(console), DB.new)
conman.run